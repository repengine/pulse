# Pulse IRIS Plugin Dependencies
# Install with: pip install -r plugin_requirements.txt

# Core dependencies
requests>=2.28.0
pandas>=1.5.0

# Alpha Vantage API
# No additional requirements, uses requests

# Open-Meteo API
# No additional requirements, uses requests

# World Bank API
# No additional requirements, uses requests

# Hacker News API
# No additional requirements, uses requests

# Reddit API
# No additional requirements, uses requests

# WHO Global Health Observatory API
# No additional requirements, uses requests

# NewsAPI
# No additional requirements, uses requests

# GitHub API
# No additional requirements, uses requests

# Google Trends API
pytrends>=4.8.0